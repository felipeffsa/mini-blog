[pytest]
DJANGO_SETTINGS_MODULE = person.settings
python_files = test.py tests.py test_*.py tests_*.py *_test.py *_tests.py

addopts = 
    --doctest-modules
    --strict-markers
    -rP

markers = 
    slow: Run tests that are slow to
    fast: Run fast tests